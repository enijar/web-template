import { styled } from "styled-components";

export const Wrapper = styled.main`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  max-width: 24rem;
  margin: 0 auto;
  padding: 2rem 1rem;
`;
