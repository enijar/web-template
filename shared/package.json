{
  "name": "@app/shared",
  "private": true,
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "start": "NODE_ENV=development tsc -p tsconfig.json --watch",
    "build": "NODE_ENV=production tsc -p tsconfig.json"
  },
  "devDependencies": {
    "prettier": "^2.3.0",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "use-model-validation": "^0.1.6"
  }
}
