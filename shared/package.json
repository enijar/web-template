{
  "name": "@app/shared",
  "private": true,
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "start": "NODE_ENV=development tsc -p tsconfig.json --watch",
    "build": "NODE_ENV=production tsc -p tsconfig.json"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "use-model-validation": "^0.2.6"
  }
}
