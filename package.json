{
  "private": true,
  "scripts": {
    "postinstall": "concurrently -n shared,client \"npm --prefix shared install\" \"npm --prefix client install\"",
    "start": "concurrently -n shared,client \"npm --prefix shared start\" \"npm --prefix client start\"",
    "build": "concurrently -n shared,client \"npm --prefix shared run build\" \"npm --prefix client run build\""
  },
  "devDependencies": {
    "concurrently": "^6.1.0",
    "prettier": "^2.3.0"
  }
}
